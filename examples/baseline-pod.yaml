apiVersion: v1
kind: Pod
metadata:
  name: baseline-web
  labels:
    app: baseline-web
spec:
  containers:
    - name: web
      image: nginx:1.27
      ports:
        - containerPort: 8080
      securityContext:
        capabilities:
          add:
            - CHOWN
            - NET_BIND_SERVICE
